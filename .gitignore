node_modules
.turbo
.next
dist
build
.env
.env.local
.env.*
!.env.example
coverage
playwright-report
test-results
*.log
data
tmp
.pnpm-store/*
.cursor/

# PGlite can create this literal directory name if :memory: is resolved as a relative path (e.g. wrong cwd).
packages/db/:memory:/
.tmp/*
